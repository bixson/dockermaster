apiVersion: v1
kind: Service
metadata:
  name: dockermaster
  labels:
    app: dockermaster
spec:
  selector:
    app: dockermaster
  ports:
    - protocol: TCP
      port: 80
      targetPort: 7681
  type: ClusterIP
